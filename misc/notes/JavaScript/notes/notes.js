// Nums
4
-10.5

// Strings (double OR single)
'Hello'
"Hello"
"hello".length

// Booleans
true
false

// null: literally nothing
null

// undefined: something has no value, yet
undefined

// Dynamic vars
var name = "Rusty";
var secret = 73;
var isA = true;

// Gives annoying pop-up
alert()

// Prints to JS console only
console.log()

// Prompts the user for input  (kinda like scanf())
prompt()

// Compares only value (type coercion)
==
!=

// Compares value AND type (preferred)
===
!==


// Function declarations:
  // 2 types:
//    Declaration
//    Expression

// Type 1
function functionName(arg1, arg2) {
  return "blah";
}

// Type 2
var someting = function(anotherArg) {
  return "blahgain";
}


// Add to end of array, return length of array
array.push()

// Remove from end of array, return removed item.
array.pop()

// Add to beginning of array
array.unshift()

// Remove from beginning of array.
array.shift()
